#!/bin/bash

# value=$(light | sed 's/\(\([0-9]\+\)\.\([0-9]\+\)\)\([^0-9].*\)\?/\2/')

if [ $1 = "up" ]
then
        light -A 5
        # value=$(light)
        # notify-send -u low -t 1000 -r 1234123 "Backlight" "$value%" -i /home/sahil/.config/repos/personal/icons/sunrise64.png
else
        light -U 5
        # value=$(light)
        # notify-send -u low -t 1000 -r 1234123 "Backlight" "$value%" -i /home/sahil/.config/repos/personal/icons/sunset64.png
fi

